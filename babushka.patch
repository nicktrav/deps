diff --git a/deps/pkg_managers.rb b/deps/pkg_managers.rb
index 1e27bad1..c2da24b2 100644
--- a/deps/pkg_managers.rb
+++ b/deps/pkg_managers.rb
@@ -10,7 +10,6 @@ dep 'apt' do
   requires 'package manager'.with('apt-get')
   requires {
     on :ubuntu, 'apt source'.with(:repo => 'main'), 'apt source'.with(:repo => 'universe')
-    on :debian, 'apt source'.with(:repo => 'main')
   }
 end
